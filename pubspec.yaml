name: xylophone
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  english_words: ^4.0.0
  cupertino_icons: ^1.0.6
  audioplayers: ^5.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/
